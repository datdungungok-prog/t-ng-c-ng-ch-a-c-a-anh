<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gửi Công Chúa Của Anh</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #ffebee;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
        }

        /* Hiệu ứng trái tim bay */
        .heart {
            position: absolute;
            color: #ff80ab;
            font-size: 20px;
            user-select: none;
            animation: move 4s linear infinite;
            opacity: 0;
        }

        @keyframes move {
            0% { transform: translateY(100vh) scale(0); opacity: 1; }
            100% { transform: translateY(-10vh) scale(1.5); opacity: 0; }
        }

        /* Khung nội dung */
        .card {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            z-index: 10;
            max-width: 400px;
            border: 2px solid #ff80ab;
        }

        h1 { color: #d81b60; font-size: 24px; }
        p { color: #555; line-height: 1.6; font-size: 18px; }
        .btn {
            background-color: #ff80ab;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 50px;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
            display: inline-block;
            margin-top: 20px;
        }
        .btn:hover { background-color: #f06292; transform: scale(1.1); }
    </style>
</head>
<body>

    <div class="card">
        <h1>Gửi Thanh Như ❤️</h1>
        <p>Đừng buồn nữa nhé, vì lúc nào cũng có anh ở bên cạnh em mà. Em là điều tuyệt vời nhất thế giới này!</p>
        <p><i>"Cười lên một cái cho thế giới bừng sáng đi nào!"</i></p>
        <button class="btn" onclick="sayLove()">Nhấn vào đây nè</button>
    </div>

    <script>
        // Tạo trái tim bay ngẫu nhiên
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 2 + 3 + 's';
            document.body.appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 5000);
        }
        setInterval(createHeart, 3000);

        function sayLove() {
            alert("Anh thương em nhất trên đời! Đi ăn kem/trà sữa không nè? 😍");
        }
    </script>
</body>
</html>